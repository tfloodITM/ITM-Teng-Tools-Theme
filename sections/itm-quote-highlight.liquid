{%- comment -%}
  ITM Quote Highlight
  -------------------
  A single big standout pull-quote section, matching the style Teng Tools'
  own site (tengtools.com) uses for its two customer testimonials — large
  grey quotation mark, big quote text, circular headshot + name/title below.

  This is deliberately NOT the horizontal multi-card review carousel
  (itm-ws-reviews.liquid) — no star ratings, no "helpful" votes, no
  scrolling strip of cards. With 2+ blocks it quietly crossfades between
  them on a timer with small dot indicators (exactly like the reference
  site), never a swipeable carousel. With exactly 1 block it's just a
  static quote, no dots, no timer.
{%- endcomment -%}

{%- liquid
  assign sid = section.id
  assign block_count = section.blocks.size
-%}

{%- style -%}
  #shopify-section-{{ sid }} .itm-qh {
    background: {{ section.settings.bg_color }};
    padding: {{ section.settings.padding_top }}px 20px {{ section.settings.padding_bottom }}px;
  }
  #shopify-section-{{ sid }} .itm-qh__inner {
    max-width: {{ section.settings.max_width | default: 760 }}px;
    margin: 0 auto;
    text-align: left;
    position: relative;
  }
  #shopify-section-{{ sid }} .itm-qh__stage {
    position: relative;
  }
  #shopify-section-{{ sid }} .itm-qh__slide {
    display: none;
  }
  #shopify-section-{{ sid }} .itm-qh__slide.is-active {
    display: block;
    animation: itm-qh-fade .5s ease;
  }
  @keyframes itm-qh-fade {
    from { opacity: 0; }
    to   { opacity: 1; }
  }
  #shopify-section-{{ sid }} .itm-qh__mark {
    display: block;
    font-family: Georgia, 'Times New Roman', serif;
    font-size: {{ section.settings.mark_size_desktop }}px;
    line-height: 1;
    color: {{ section.settings.mark_color }};
    margin-bottom: {{ section.settings.mark_spacing_desktop }}px;
  }
  #shopify-section-{{ sid }} .itm-qh__quote {
    font-size: {{ section.settings.quote_size_desktop }}px;
    line-height: 1.45;
    color: {{ section.settings.text_color }};
    margin: 0 0 24px;
  }
  #shopify-section-{{ sid }} .itm-qh__person {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: 12px;
  }
  #shopify-section-{{ sid }} .itm-qh__avatar {
    width: {{ section.settings.avatar_size_desktop }}px;
    height: {{ section.settings.avatar_size_desktop }}px;
    border-radius: 50%;
    object-fit: cover;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
    font-weight: 700;
    font-size: {{ section.settings.avatar_size_desktop | times: 0.3 | round }}px;
    text-transform: uppercase;
  }
  #shopify-section-{{ sid }} .itm-qh__person-text {
    text-align: left;
  }
  #shopify-section-{{ sid }} .itm-qh__name {
    display: block;
    font-weight: 700;
    font-size: {{ section.settings.name_size_desktop }}px;
    color: {{ section.settings.text_color }};
  }
  #shopify-section-{{ sid }} .itm-qh__title {
    display: block;
    font-size: {{ section.settings.subline_size_desktop }}px;
    color: {{ section.settings.subtext_color }};
  }
  @media (max-width: 749px) {
    #shopify-section-{{ sid }} .itm-qh__mark {
      font-size: {{ section.settings.mark_size_mobile }}px;
      margin-bottom: {{ section.settings.mark_spacing_mobile }}px;
    }
    #shopify-section-{{ sid }} .itm-qh__quote {
      font-size: {{ section.settings.quote_size_mobile }}px;
    }
    #shopify-section-{{ sid }} .itm-qh__avatar {
      width: {{ section.settings.avatar_size_mobile }}px;
      height: {{ section.settings.avatar_size_mobile }}px;
      font-size: {{ section.settings.avatar_size_mobile | times: 0.3 | round }}px;
    }
    #shopify-section-{{ sid }} .itm-qh__name {
      font-size: {{ section.settings.name_size_mobile }}px;
    }
    #shopify-section-{{ sid }} .itm-qh__title {
      font-size: {{ section.settings.subline_size_mobile }}px;
    }
  }
  #shopify-section-{{ sid }} .itm-qh__dots {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    gap: 8px;
    margin-top: 28px;
  }
  #shopify-section-{{ sid }} .itm-qh__dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    padding: 0;
    border: 1.5px solid {{ section.settings.mark_color }};
    background: transparent;
    cursor: pointer;
  }
  #shopify-section-{{ sid }} .itm-qh__dot.is-on {
    background: {{ section.settings.text_color }};
    border-color: {{ section.settings.text_color }};
  }
{%- endstyle -%}

<div class="itm-qh">
  <div class="itm-qh__inner">
    {%- if section.settings.heading != blank -%}
      <h2 class="itm-qh__heading">{{ section.settings.heading }}</h2>
    {%- endif -%}

    <div class="itm-qh__stage" data-qh-stage="{{ sid }}">
      {%- for block in section.blocks -%}
        <div class="itm-qh__slide{% if forloop.first %} is-active{% endif %}" data-qh-slide {{ block.shopify_attributes }}>
          <span class="itm-qh__mark" aria-hidden="true">&ldquo;</span>
          <p class="itm-qh__quote">{{ block.settings.quote }}</p>
          <div class="itm-qh__person">
            {%- if block.settings.photo != blank -%}
              <img class="itm-qh__avatar" src="{{ block.settings.photo | image_url: width: 240 }}" alt="" loading="lazy">
            {%- else -%}
              <div class="itm-qh__avatar" style="background:{{ block.settings.avatar_color }}">{{ block.settings.initials }}</div>
            {%- endif -%}
            <div class="itm-qh__person-text">
              <strong class="itm-qh__name">{{ block.settings.name }}</strong>
              <span class="itm-qh__title">{{ block.settings.title }}</span>
            </div>
          </div>
        </div>
      {%- endfor -%}
    </div>

    {%- if block_count > 1 -%}
      <div class="itm-qh__dots" data-qh-dots="{{ sid }}">
        {%- for block in section.blocks -%}
          <button type="button" class="itm-qh__dot{% if forloop.first %} is-on{% endif %}" data-qh-dot="{{ forloop.index0 }}" aria-label="Show quote {{ forloop.index }}"></button>
        {%- endfor -%}
      </div>
    {%- endif -%}
  </div>
</div>

{%- if block_count > 1 -%}
  <script>
  (function () {
    var stage = document.querySelector('[data-qh-stage="{{ sid }}"]');
    if (!stage) return;
    var slides = Array.from(stage.querySelectorAll('[data-qh-slide]'));
    var dotsWrap = document.querySelector('[data-qh-dots="{{ sid }}"]');
    var dots = dotsWrap ? Array.from(dotsWrap.querySelectorAll('[data-qh-dot]')) : [];
    var current = 0;
    var seconds = {{ section.settings.autoplay_seconds | default: 6 }};
    var timer = null;

    function show(idx) {
      current = idx;
      slides.forEach(function (s, i) { s.classList.toggle('is-active', i === idx); });
      dots.forEach(function (d, i) { d.classList.toggle('is-on', i === idx); });
    }
    function next() {
      show((current + 1) % slides.length);
    }
    function restart() {
      if (timer) clearInterval(timer);
      if (seconds > 0) timer = setInterval(next, seconds * 1000);
    }
    dots.forEach(function (d, i) {
      d.addEventListener('click', function () {
        show(i);
        restart();
      });
    });
    restart();
  })();
  </script>
{%- endif -%}

{% schema %}
{
  "name": "ITM — Quote highlight",
  "tag": "section",
  "class": "section",
  "max_blocks": 6,
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading (optional)"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background colour",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Quote / name colour",
      "default": "#1a1a1a"
    },
    {
      "type": "color",
      "id": "subtext_color",
      "label": "Title / role colour",
      "default": "#666666"
    },
    {
      "type": "color",
      "id": "mark_color",
      "label": "Quotation mark / dots colour",
      "default": "#c9c9c9"
    },
    {
      "type": "number",
      "id": "max_width",
      "label": "Max content width (px)",
      "default": 760
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "min": 0,
      "max": 120,
      "step": 4,
      "default": 64
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "default": 64
    },
    {
      "type": "range",
      "id": "autoplay_seconds",
      "label": "Seconds between quotes",
      "info": "Only used when there are 2 or more quotes. Set 0 to disable auto-rotation (dots still work by click).",
      "min": 0,
      "max": 15,
      "step": 1,
      "default": 6
    },
    {
      "type": "header",
      "content": "Quotation mark size"
    },
    {
      "type": "range",
      "id": "mark_size_desktop",
      "label": "Desktop (px)",
      "min": 20,
      "max": 120,
      "step": 2,
      "default": 64
    },
    {
      "type": "range",
      "id": "mark_size_mobile",
      "label": "Mobile (px)",
      "min": 16,
      "max": 100,
      "step": 2,
      "default": 40
    },
    {
      "type": "header",
      "content": "Space between quotation mark and quote text"
    },
    {
      "type": "range",
      "id": "mark_spacing_desktop",
      "label": "Desktop (px)",
      "info": "Can go negative to pull the quote text up closer to the mark.",
      "min": -80,
      "max": 60,
      "step": 2,
      "default": 4
    },
    {
      "type": "range",
      "id": "mark_spacing_mobile",
      "label": "Mobile (px)",
      "info": "Can go negative to pull the quote text up closer to the mark.",
      "min": -80,
      "max": 60,
      "step": 2,
      "default": 4
    },
    {
      "type": "header",
      "content": "Quote text size"
    },
    {
      "type": "range",
      "id": "quote_size_desktop",
      "label": "Desktop (px)",
      "min": 12,
      "max": 40,
      "step": 1,
      "default": 26
    },
    {
      "type": "range",
      "id": "quote_size_mobile",
      "label": "Mobile (px)",
      "min": 12,
      "max": 32,
      "step": 1,
      "default": 18
    },
    {
      "type": "header",
      "content": "Photo / avatar size"
    },
    {
      "type": "range",
      "id": "avatar_size_desktop",
      "label": "Desktop (px)",
      "min": 24,
      "max": 120,
      "step": 2,
      "default": 48
    },
    {
      "type": "range",
      "id": "avatar_size_mobile",
      "label": "Mobile (px)",
      "min": 24,
      "max": 100,
      "step": 2,
      "default": 40
    },
    {
      "type": "header",
      "content": "Name text size"
    },
    {
      "type": "range",
      "id": "name_size_desktop",
      "label": "Desktop (px)",
      "min": 10,
      "max": 28,
      "step": 1,
      "default": 15
    },
    {
      "type": "range",
      "id": "name_size_mobile",
      "label": "Mobile (px)",
      "min": 10,
      "max": 24,
      "step": 1,
      "default": 14
    },
    {
      "type": "header",
      "content": "Subline (title / role) text size"
    },
    {
      "type": "range",
      "id": "subline_size_desktop",
      "label": "Desktop (px)",
      "min": 9,
      "max": 24,
      "step": 1,
      "default": 13
    },
    {
      "type": "range",
      "id": "subline_size_mobile",
      "label": "Mobile (px)",
      "min": 9,
      "max": 20,
      "step": 1,
      "default": 12
    }
  ],
  "blocks": [
    {
      "type": "quote",
      "name": "Quote",
      "settings": [
        {
          "type": "textarea",
          "id": "quote",
          "label": "Quote",
          "default": "This tool changed the way we work."
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name",
          "default": "Customer"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title / role"
        },
        {
          "type": "image_picker",
          "id": "photo",
          "label": "Photo (optional)",
          "info": "Falls back to initials below if left blank."
        },
        {
          "type": "text",
          "id": "initials",
          "label": "Fallback initials (2 letters)",
          "default": "CU"
        },
        {
          "type": "color",
          "id": "avatar_color",
          "label": "Fallback avatar colour",
          "default": "#4a7cb5"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "ITM — Quote highlight",
      "blocks": [
        {
          "type": "quote",
          "settings": {
            "quote": "When you've got ten seconds to change everything, your tools can't fail. That's why we trust Teng Tools.",
            "name": "Jake Matthews",
            "title": "Race Technician, T-Sport Bernau",
            "initials": "JM",
            "avatar_color": "#4a7cb5"
          }
        },
        {
          "type": "quote",
          "settings": {
            "quote": "Teng Tools has given us a controlled environment — everything is consistent, traceable, and easy to manage across the site.",
            "name": "Graham Finch",
            "title": "Senior Maintenance Excellence Manager",
            "initials": "GF",
            "avatar_color": "#7a8ea0"
          }
        }
      ]
    }
  ]
}
{% endschema %}
