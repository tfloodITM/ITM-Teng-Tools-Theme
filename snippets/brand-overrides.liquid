{%- comment -%}
  ================================================================
  Teng Tools Global Visual Overrides
  (renamed from worksharp-overrides.liquid during the Teng Tools rebrand;
  originally converted from worksharp-frontend-overrides.js)
  ----------------------------------------------------------------
  NOTE: The header width/padding and the header search bar are now
  controlled by the Header section settings:
    • "Header max width"          (Header section → Header width)
    • "Header side padding"       (Header section → padding options)
    • search bar size / styling   (header section CSS)
  The old hard-coded overrides (max-width:2560px, 150px padding,
  .ws-search max-width:380px) were removed so those settings
  actually take effect. Re-add store-specific overrides below only
  if you need something the section settings can't express.
  ================================================================
{%- endcomment -%}
